<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.micro.platform.system.mapper.SysOperationLogMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.micro.platform.system.entity.SysOperationLog">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="business_type" property="businessType"/>
        <result column="method" property="method"/>
        <result column="request_method" property="requestMethod"/>
        <result column="operator_type" property="operatorType"/>
        <result column="oper_name" property="operName"/>
        <result column="dept_name" property="deptName"/>
        <result column="oper_url" property="operUrl"/>
        <result column="oper_ip" property="operIp"/>
        <result column="oper_location" property="operLocation"/>
        <result column="oper_param" property="operParam"/>
        <result column="json_result" property="jsonResult"/>
        <result column="status" property="status"/>
        <result column="error_msg" property="errorMsg"/>
        <result column="oper_time" property="operTime"/>
        <result column="cost_time" property="costTime"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, title, business_type, method, request_method, operator_type, oper_name, dept_name, oper_url, oper_ip, oper_location, oper_param, json_result, status, error_msg, oper_time, cost_time
    </sql>

    <!-- 分页查询操作日志列表 -->
    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_operation_log
        <where>
            1=1
            <if test="log.title != null and log.title != ''">
                AND title LIKE CONCAT('%', #{log.title}, '%')
            </if>
            <if test="log.operName != null and log.operName != ''">
                AND oper_name LIKE CONCAT('%', #{log.operName}, '%')
            </if>
            <if test="log.businessType != null">
                AND business_type = #{log.businessType}
            </if>
            <if test="log.status != null">
                AND status = #{log.status}
            </if>
            <if test="log.operTime != null">
                AND oper_time &gt;= #{log.operTime}
            </if>
        </where>
        ORDER BY oper_time DESC
    </select>

    <!-- 清空操作日志 -->
    <update id="clean">
        TRUNCATE TABLE sys_operation_log
    </update>

</mapper>