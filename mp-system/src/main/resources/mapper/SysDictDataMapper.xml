<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.micro.platform.system.mapper.SysDictDataMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.micro.platform.system.entity.SysDictData">
        <id column="dict_code" property="dictCode"/>
        <result column="dict_type" property="dictType"/>
        <result column="dict_label" property="dictLabel"/>
        <result column="dict_value" property="dictValue"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="css_class" property="cssClass"/>
        <result column="list_class" property="listClass"/>
        <result column="is_default" property="isDefault"/>
        <result column="remark" property="remark"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        dict_code, dict_type, dict_label, dict_value, sort, status, css_class, list_class, is_default, remark, create_by, create_time, update_by, update_time, deleted
    </sql>

    <!-- 分页查询字典数据列表 -->
    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_dict_data
        <where>
            deleted = 0
            <if test="dict.dictType != null and dict.dictType != ''">
                AND dict_type = #{dict.dictType}
            </if>
            <if test="dict.dictLabel != null and dict.dictLabel != ''">
                AND dict_label LIKE CONCAT('%', #{dict.dictLabel}, '%')
            </if>
            <if test="dict.status != null">
                AND status = #{dict.status}
            </if>
        </where>
        ORDER BY sort ASC
    </select>

    <!-- 根据字典类型查询字典数据 -->
    <select id="selectByDictType" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_dict_data
        WHERE dict_type = #{dictType} AND deleted = 0 AND status = 1
        ORDER BY sort ASC
    </select>

</mapper>