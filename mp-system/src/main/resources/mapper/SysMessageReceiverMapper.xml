<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.micro.platform.system.mapper.SysMessageReceiverMapper">

    <insert id="batchInsert">
        INSERT INTO sys_message_receiver (id, message_id, receiver_id, receiver_name, is_read, create_time)
        VALUES
        <foreach collection="receiverIds" item="receiverId" separator=",">
            (
                #{receiverId},
                #{messageId},
                #{receiverId},
                (SELECT username FROM sys_user WHERE user_id = #{receiverId}),
                0,
                NOW()
            )
        </foreach>
    </insert>

</mapper>