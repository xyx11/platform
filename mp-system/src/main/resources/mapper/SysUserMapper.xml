<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.micro.platform.system.mapper.SysUserMapper">

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultType="com.micro.platform.system.entity.SysUser">
        SELECT * FROM sys_user WHERE username = #{username} AND deleted = 0
    </select>

    <!-- 根据手机号查询用户 -->
    <select id="selectByPhone" resultType="com.micro.platform.system.entity.SysUser">
        SELECT * FROM sys_user WHERE phone = #{phone} AND deleted = 0
    </select>

    <!-- 根据邮箱查询用户 -->
    <select id="selectByEmail" resultType="com.micro.platform.system.entity.SysUser">
        SELECT * FROM sys_user WHERE email = #{email} AND deleted = 0
    </select>

    <!-- 查询用户的角色 ID 列表 -->
    <select id="selectRoleIdsByUserId" resultType="java.lang.Long">
        SELECT role_id FROM sys_user_role WHERE user_id = #{userId}
    </select>

    <!-- 查询用户的权限标识列表 -->
    <select id="selectPermissionsByUserId" resultType="java.lang.String">
        SELECT DISTINCT m.permission
        FROM sys_menu m
        INNER JOIN sys_role_menu rm ON m.menu_id = rm.menu_id
        INNER JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND m.permission IS NOT NULL
          AND m.permission != ''
          AND m.deleted = 0
    </select>

    <!-- 根据角色 ID 查询用户列表 -->
    <select id="selectUsersByRoleId" resultType="com.micro.platform.system.entity.SysUser">
        SELECT u.*
        FROM sys_user u
        INNER JOIN sys_user_role ur ON u.user_id = ur.user_id
        WHERE ur.role_id = #{roleId} AND u.deleted = 0
    </select>

    <!-- 批量删除用户角色关联 -->
    <delete id="deleteUserRoleBatch">
        DELETE FROM sys_user_role WHERE role_id = #{roleId}
    </delete>

    <!-- 批量插入用户角色关联 -->
    <insert id="batchInsertUserRole">
        INSERT INTO sys_user_role (role_id, user_id) VALUES
        <foreach collection="userIds" item="userId" separator=",">
            (#{roleId}, #{userId})
        </foreach>
    </insert>

    <!-- 批量删除用户角色关联（指定用户） -->
    <delete id="batchDeleteUserRole">
        DELETE FROM sys_user_role WHERE role_id = #{roleId} AND user_id IN
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </delete>

</mapper>